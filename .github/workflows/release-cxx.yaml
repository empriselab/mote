name: Release FFI C++ Library

on:
  push:
    tags:
      - 'mote-cxx-v*.*.*'

env:
  CARGO_TERM_COLOR: always

defaults:
  run:
    shell: bash

jobs:
  release-ffi-cxx:
    permissions:
      contents: write
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            recipe: release-package-cxx-linux
            docker_platform: linux/amd64
          - os: ubuntu-latest
            recipe: release-package-cxx-linux-aarch64
            docker_platform: linux/arm64
          - os: macos-latest
            recipe: release-package-cxx-macos
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: Swatinem/rust-cache@v2
        if: matrix.os == 'macos-latest'
        with:
          workspaces: mote-ffi

      - name: Install Task
        uses: go-task/setup-task@v2

      - name: Check version matches tag
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#mote-cxx-v}"
          CRATE_VERSION=$(grep '^version' mote-ffi/Cargo.toml | head -1 | sed 's/version = "\(.*\)"/\1/')
          if [ "$TAG_VERSION" != "$CRATE_VERSION" ]; then
            echo "Error: Tag version ($TAG_VERSION) does not match crate version ($CRATE_VERSION)"
            exit 1
          fi
          echo "Version check passed: $CRATE_VERSION"

      - name: Set up QEMU
        if: matrix.docker_platform == 'linux/arm64'
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Build and package
        working-directory: mote-ffi
        run: task ${{ matrix.recipe }}

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          files: mote-ffi/dist/*.tar.gz
